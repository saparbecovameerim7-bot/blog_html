services:
  type: web
  name: blog_dj
  runtime: python
  plan: free
  buildCommand: "pip install -r requirements.txt"
  startCommand: "gunicorn blog.wsgi:application --workers 2 --bind 0.0.0.0:$PORT"
  envVars:
    key: PYTHON_VERSION
    value: 3.12.1
